import type { CustomFlowbiteTheme } from 'flowbite-react';

const textInputStyling: CustomFlowbiteTheme['textInput'] = {
  field: {
    input: {
      sizes: {
        lg: 'text-lg px-1 py-1 font-medium',
      },
    },
  },
};

export default textInputStyling;
